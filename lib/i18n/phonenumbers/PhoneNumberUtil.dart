class PhoneNumberUtil {
  static const String REGION_CODE_FOR_NON_GEO_ENTITY = "001";

  // final Map<String, PhoneMetadata> regionToMetadataMap;
  // static final PhoneNumberUtil _singleton = PhoneNumberUtil();

  // PhoneNumberUtil({
  //   this.regionToMetadataMap = const {},
  // });

  // factory PhoneNumberUtil.instance() {
  //   return _singleton;
  // }

  // int getCountryCodeForRegion(String regionCode) {
  //   if (!_isValidRegionCode(regionCode)) return 0;

  //   return _getCountryCodeForValidRegion(regionCode);
  // }

//   bool _isValidRegionCode(String regionCode) {
// return regionCode != null &&
//       isNaN(regionCode) &&
//       regionCode.toUpperCase() in i18n.phonenumbers.metadata.countryToMetadata;
//   }
}
